name: (disabled) Release via tag

on:
  push:
    # Desativado: deixamos o padrão impossível de casar
    tags-ignore:
      - '**'

jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release por tag desativado. Use Release Please."
